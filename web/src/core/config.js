/**
 * 网站配置文件
 */

const config = {
  appName: '泾县农商行',
  appLogo: '/static/icon/logo.jpeg',
  showViteLogo: false,
  logs: [],
}

export const viteLogo = (env) => {
  if (config.showViteLogo) {
    const chalk = require('chalk')
  }
}

export default config
